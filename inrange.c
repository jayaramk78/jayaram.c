#include<stdio.h>
int main()
{
    int i,j,k;
    printf("\n Enter the no:");
    scanf("%d%d%d",&i,&j,&k);
  if(i>=j && i<=k)  
{
    printf("\n Yes it is in range");
    }
    else
    printf("\n It is not in range");
}
